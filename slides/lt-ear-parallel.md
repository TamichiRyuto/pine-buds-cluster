# 耳で並列計算してみた

**夏休みの自由研究 — ワイヤレスイヤホンを HPC クラスタにする**

LT 8 分 / 全 15 枚 (+ 表紙)

> このファイルが原稿の source of truth。`scripts/make_lt_pptx.py` が
> `slides/lt-ear-parallel.pptx` を生成する。数字を直すときは**両方**を直す。
>
> **pptx は投影用に文字を大きくしてあり、ここより内容を絞ってある。**
> md 側に残してある細かい数字・ログ行は口頭で補うか、質疑用の手札として使う。

---

## 1. 自己紹介 & サマリ

**発表者**

- 名前: `[ ここに名前 ]`
- 所属: `[ ここに所属 ]`
- 普段: `[ ここに普段やっていること ]`
- GitHub: `github.com/TamichiRyuto/pine-buds-cluster`

**3 行サマリ**

1. 左右のワイヤレスイヤホン **2 個を 2 ノードの計算クラスタ**にした
2. MPI (プロセス間並列) のサブセットを自作し、**イヤホン同士の Bluetooth リンク (IBRT)** に載せた
3. **標準 MPI + OpenMP で書いた GEMM ベンチを 1 行も変えずに**実機で PASS させた
   ノード間は Bluetooth、ノード内は第 2 コア (CP)。**計 4 コアで 1 つの行列積**

**成果 (実測)**

| 項目 | 値 |
|---|---|
| ノード数 | 2 (右バッズ = rank 0 / 左バッズ = rank 1) × 各 2 コア = 計 4 コア |
| ベンチ | GEMM `C = A·B` N=32 float32 |
| 判定 | `checksum=32768.000000` (= 32³) 厳密一致で PASS |
| 並列領域 | 3,783 µs → 1,226 µs (**x3.1**、Run 16) |
| 全体 elapsed | mpi = 3,683 µs (x1.02) / mpi+omp = 313,258 µs (通信待ちに支配される) |
| 左右間ペイロード | 実測 512 B (ヘッダは 300、バイナリの assert は 672) |
| 観測 | Bluetooth SPP でログを PC に配信 / 5 連タップで再実行 |
| 期間 / コミット | 2026-08-18 〜 09-01 (約 2 週間) / 112 コミット |

> Note: つかみは「イヤホンはコンピュータである」。BES2300YP は Cortex-M4F が 2 個載った
> れっきとした MCU。音を鳴らしていない時間、CPU は暇をしている。

---

## 2. 並列計算のおさらい — MPI と OpenMP

**図解: 並列化には 2 つのレベルがある**

```
┌─────────── ノード A (メモリ空間 A) ───────────┐   ┌─────────── ノード B (メモリ空間 B) ───────────┐
│  ┌────────┐  ┌────────┐                      │   │  ┌────────┐  ┌────────┐                      │
│  │ コア 0 │  │ コア 1 │  ← スレッドレベル並列 │   │  │ コア 0 │  │ コア 1 │                      │
│  └────────┘  └────────┘     = OpenMP          │   │  └────────┘  └────────┘                      │
│        ＼        ／                           │   │        ＼        ／                           │
│      同じ配列を共有 (共有メモリ)              │   │      同じ配列を共有                           │
└───────────────────────────────────────────────┘   └───────────────────────────────────────────────┘
                    ↑                                                   ↑
                    └──────── クラスタレベル並列 = MPI ─────────────────┘
                       メモリが物理的に別。明示的にメッセージを送り合う
                       MPI_Send / MPI_Recv / MPI_Barrier / MPI_Allreduce
```

| | OpenMP | MPI |
|---|---|---|
| 単位 | スレッド | プロセス (ランク) |
| メモリ | 共有 | 分散 (別空間) |
| 書き方 | `#pragma omp parallel for` | `MPI_Send` / `MPI_Recv` |
| コスト | 安い (ns〜μs) | 高い (μs〜ms、ネットワーク次第) |
| 今回の担当 | イヤホン**内**の 2 コア (**実測 x3.1**) | イヤホン**間** (左 ⇄ 右) |

**GEMM (行列積) を 2 ランクに分けると**

まず用語 (`bench/gemm_mpi_omp.cpp` の定義):

- **`N`** = 行列の一辺。`A` `B` `C` はすべて `N×N` の正方行列 (row-major)。`N=32` なら 32×32
- **`size`** = `MPI_Comm_size` の値 = 参加ランク数 = **参加したバッズの数**。2 なら左右が揃った、
  1 なら TWS リンクが張れず片側だけの縮退
- **`rank`** = `MPI_Comm_rank` の値 = 自分の番号。右 = 0 / 左 = 1 (左右ストラップ由来で固定)

```
C = A · B      A の行を [m0, m1) で分割
   m0 = rank * N / size ,  m1 = (rank + 1) * N / size
rank 0 → 行 0..15 を計算    rank 1 → 行 16..31 を計算
                ↓ MPI_Send / MPI_Recv で結果を集約 ↓
             rank 0 が全体の checksum を検証して PASS/FAIL
```

`A` `B` は全要素 `1.0f` なので `C[i][j]` はちょうど `N`、checksum は `N³ = 32768`。

**注意**: `size=1` に縮退しても 1 ランクが全行を計算するだけなので **同じ checksum で PASS が出る**。
だから合否条件に「両側 `size=2`」を独立に入れてある (実際 Run 2 は縮退した `size=1` で PASS していた)。

> Note: HPC の定石「ノード間は MPI、ノード内は OpenMP」のハイブリッド構成。
> 今回はこの標準的な書き方をそのまま持ち込むのがテーマ。OpenMP 側は API だけ揃えた段階。

---

## 3. 今回のクラスタ構成

```
        ┌───────────────────────┐   Bluetooth (IBRT / BESAUD)   ┌───────────────────────┐
        │  右バッズ  rank 0     │◀────── L2CAP 上の TWS ───────▶│  左バッズ  rank 1     │
        │  BES2300YP            │        制御チャネル            │  BES2300YP            │
        │  Cortex-M4F ×2        │   cmdcode 0x8201 (自作)        │  Cortex-M4F ×2        │
        └───────────────────────┘                                └───────────────────────┘
                    │                                                        │
                    └────────── Bluetooth SPP (自作ログチャネル) ────────────┘
                                              ▼
                                    Windows PC (COM6)
                                  #23 GEMM-MPI ... PASS
```

- **ノード間 = MPI**: 自作 MPI サブセットを IBRT (イヤホン同士の TWS リンク) に載せた
- **ノード内 = OpenMP**: 第 2 コア (CP) で並列領域を実行。並列領域だけ見れば x3.1 (Run 16)
- **観測 = SPP**: Bluetooth 上のシリアルポート。UART とは独立の経路

> Note: rank は左右ストラップ由来で固定だが、**IBRT のロール (master/slave) は起動ごとに入れ替わる**。
> スライドの MASTER/SLAVE 表記は nv_role (不揮発の初期値)。この違いが §11 の「slave のログが
> 電波に乗らない」問題に効いてくる。

---

## 4. 実験機材: PineBuds Pro

| 項目 | 値 |
|---|---|
| SoC | Bestechnic **BES2300YP** (左右に 1 個ずつ、完全に独立) |
| CPU | **Dual-core ARM Cortex-M4F @ 最大 300 MHz** |
| FPU | **単精度のみ** (`double` はソフトエミュ = 実質禁止) |
| メモリ | SRAM 992 KB (+ BT 共有 64 KB) / Flash 4 MB |
| ファーム | **OpenPineBuds** (pine64) — **アプリ層は OSS** (BT スタック中核はプリビルト `.a`) |
| ビルド | Docker + Make、`gnu++98` / `-fno-exceptions` / `-fno-rtti` |
| デバッグ | 充電ケースが **USB → デュアル UART** を兼ねる (2,000,000 baud) |
| 実測残量 | RAM 約 330 KB / Flash 約 3.2 MB (**2026-08-30 ビルドの `.map` 集計**) |
| 位置づけ | 実売 1 万円前後の、普通に音楽が聴ける TWS イヤホン |

**つまり**: ケースに USB-C を挿すと `/dev/ttyACM0` (右) と `/dev/ttyACM1` (左) が生えて、
`bestool write-image` で自作ファームが焼ける。**イヤホンなのに開発ボードとして完結している。**

> 写真: `slides/assets/pinebuds.jpg` を置いて `scripts/make_lt_pptx.py` を再実行すると、
> pptx のスライド 5 右上の枠にアスペクト比を保ったまま自動で埋め込まれる。

> Note: 手元の個体のケースは Wiki 記載の CH342DS ではなく **CH347** だった (ハードリビジョン差)。
> どちらも CDC-ACM なので手順は同じ。「完全オープンソース」とは言わないこと — IBRT/BT スタックの
> 中核はソースが無い (これが §7 の山場そのもの)。

---

## 5. なぜ「普通のワイヤレスイヤホン」ではダメだったのか

**当初の構想**: 手持ちの TWS (左右が独立した完全ワイヤレス) でやりたかった。
→ **自作コードを載せる手段が見つからなかった。**

```
自作コードをイヤホンで動かすのに必要なもの     手元で調べた範囲の市販 TWS   PineBuds Pro
──────────────────────────────────────────────────────────────────────────────────────
① ファームウェアのソース / SDK                 非公開                  OSS (OpenPineBuds)
② 書き込み経路 (署名されていない FW を焼ける)  書き込み手段が非公開    bestool で普通に焼ける
③ 標準出力 = デバッグ UART が外に出ている       出ていない              充電ケースが UART を兼ねる
④ 左右間リンクを叩ける API                     非公開の独自 TWS        IBRT の API がヘッダで見える
⑤ 文鎮化した時の復旧手段                       見つからなかった        工場 FW + 純正ライタが配布
```

- ① 〜 ③ のどれか 1 つでも欠けると **「コードを書いても載せられない」** ので実験自体が成立しない
- 一般的な TWS は音質・ANC・電池のために作られていて、**ユーザーが計算を載せる想定がそもそも無い**
- 分解して SWD を引き出す道もあるが、**左右 2 個を同じ状態に保って何十回も焼き直す**用途には向かない
- **これは手元の機種と自分が調べられた範囲の話**で、すべての TWS がそうだと主張するものではない

**→ 「オープンソースファーム + ケースが UART プログラマ」を両立している TWS が、探した範囲で
ここだけだったので購入。**

> Note: 「Linux が動くイヤホン」ではなく「自分のコードを載せられるイヤホン」を探した、という話にする。
> 文鎮化リスクの話は共感を得やすい。実際いちばん最初にやったのは工場ファームの全量バックアップ。
> ⑤ の PineBuds 側の根拠は pine64 公式ページ (Windows programmer utility v1.48 + `AC08_20221102.bin`)。

---

## 6. やったこと — 全体像

```
┌──────────────────────────────────────────────────────────────────────┐
│  bench/gemm_mpi_omp.cpp   ← 標準 MPI + OpenMP API で書いた GEMM      │
│                              実機用に 1 行も書き換えない (これが要件) │
├──────────────────────────────────────────────────────────────────────┤
│  adapters/mpi/  自作 MPI サブセット      adapters/omp/  OpenMP スタブ │
│  Init/Send/Recv/Barrier/Allreduce        omp.h 互換 (Stage-1 は逐次)  │
│  Isend/Irecv/Wait/Waitall/Wtime                                       │
├──────────────────────────────────────────────────────────────────────┤
│  transport seam (関数ポインタ 1 枚)                                   │
│   ├─ ホスト: pthread ループバック → tests/ で TDD                     │
│   └─ 実機:  mpi_frag (断片化 + クレジット制御)                        │
├──────────────────────────────────────────────────────────────────────┤
│  firmware/pinebuds_compute/  IBRT glue / SPP ログ / 5連タップ起動     │
├──────────────────────────────────────────────────────────────────────┤
│  OpenPineBuds SDK (IBRT = TWS 制御, BESAUD, RTX RTOS)                 │
└──────────────────────────────────────────────────────────────────────┘
```

**制約 (SDK に合わせるしかない)**

- `-std=gnu++98` / `-fno-exceptions` / `-fno-rtti` → **C++11 以降が使えない**
- **ヒープ禁止・STL 禁止**・静的バッファのみ (`make check98` で機械的に担保)
- **`double` 禁止** (単精度 FPU のみ)。リテラルは全部 `1.0f`

**規模**: adapters 926 行 / firmware 統合 1,656 行 / tests 1,846 行 / 設計ドキュメント 2,643 行

> Note: レイヤ図を全幅にして、どこまでが標準 API でどこからが自作かを一目で見せる。

---

## 7. 開発環境 — 1 回の実験に必要なもの

| | 環境 | やること |
|---|---|---|
| 🐧 | **WSL2 Ubuntu 22.04** | コードを書くのは全部ここ / `make test` → 1,788 checks (g++) / `make check98` で gnu++98 方言を担保 / `install-into-sdk.sh` で SDK に配置 / git・GitHub |
| 🐳 | **Docker (OpenPineBuds)** | `./start_dev.sh` で開発コンテナ / `docker compose run --rm builder ./build.sh` → `open_source.bin` (904 KB) / `backup.sh` で工場 FW を全量退避 |
| 🪟 | **Windows 11** | `usbipd attach --wsl` で充電ケースを WSL に渡す → `/dev/ttyACM0` (右) / `ttyACM1` (左) / `spp_tail.py COM6` で Bluetooth SPP ログを受信 |
| 🎧 | **PineBuds Pro (実機)** | `bestool write-image` を左右 2 個に / 「出して 3 秒待って戻す」= リブート (書き込みトリガ) / `picocom -b 2000000 /dev/ttyACM0` / 5 連タップで再実行 |

**🤖 Claude Code — 4 つの世界を横断して回す** (103 コミット中 102 が `Co-Authored-By: Claude`)

- **設計・SDK 調査**: プリビルト `.a` の逆アセンブルと `nm` でのシンボル解決
- **TDD の運用**: Red のテストは本体セッション、Green の実装はサブエージェントへ委譲
  (`CLAUDE.md` にルールを明文化してある)
- **実機ランの監視**: UART / SPP を監視し、結果を設計ドキュメントに反映

| | 値 | 根拠 |
|---|---|---|
| 実作業時間 | **14 時間** | セッションログのイベント間隔 (10 分以内を実作業として積算)。実時間の経過は 112 時間 |
| コスト | **$490** | ログのトークン数 × 公開単価の**換算値**。87% がプロンプトキャッシュ、出力は 13% |
| コミット | **112** | うち 111 が `Co-Authored-By: Claude` |

内訳の目安: Fable 5 が約 $390 (設計・オーケストレーション)、Opus 5 が約 $72、Sonnet 5 が約 $16 (実装委譲)。
課金対象トークン 3.70 億のうち **97% がキャッシュ読み** — 長いセッションで同じ文脈を読み返した分。
9/1 の 1 日に時間の 66%・コストの 65% が集中している (実機ブリングアップを一気に片付けた日)。

**1 サイクル — 4 つの世界を 1 周しないと 1 回も試せない**

```
① 書く → ② ホストで緑 → ③ SDK に配置 → ④ ビルド → ⑤ 焼く → ⑥ 耳で観る
   ↑                                                              │
   └──────────────────────────────────────────────────────────────┘
```

⑥ で見つけた不具合は ① に戻る。② が緑にならないうちは ④ に進まない —
焼いてから気づくと左右 2 個ぶんやり直しになる。

**WSL の中の Docker の中の GCC でビルドして、Windows の Python で結果を受ける。**

> Note: 4 つの世界をまたがないと 1 回も試せない、という絵。
> 「WSL の中の Docker の中の GCC で焼いて、Windows の Python で受ける」と言うと笑いが取れる。

---

## 8. 実装の山場 ① — SDK にソースが無い

**問題**: 左右リンクの本体 `services/ibrt_core/` は **`inc/` と `lib/` しか無い**。
`src/` が存在しない = **プリビルトの `.a` しか無い**。

**やったこと**: `libtws_ibrt_enhanced_stack_anc_RTX.a` を**逆アセンブルして仕様を確定した。**

| 調べたこと | ヘッダの記述 | 逆アセンブルした実体 | 影響 |
|---|---|---|---|
| 送信ペイロード上限 | `APP_TWS_CTRL_BUFFER_MAX_LEN` = **300** | 実行時 assert は **672** | 300 と 672 のどちらで切るか設計が変わる |
| RX ハンドラのスレッド | 記述なし | **`BesbtThread`** (BT スタック本体のループ) | **ここをブロックすると BT が全部止まる** |
| 送信ポンプ | 記述なし | 同じループの 1 行上 | 計算スレッドを別に立てる必要がある |

**結論した設計**

- 計算は **専用スレッド**に隔離 (`app_init` 末尾で同期実行すると電源管理まで止まる)
- ペイロードは **実測で決める**。起動時に 4 → 64 → … → 668 B を掃引するモード (`[mpi-t1]`) を実装
  - **実測: `max_payload=512`** (668 は TIMEOUT)。ヘッダの 300 でも blob の 672 でもなかった
- **256 B を超えるメッセージは 268 B フレーム (ペイロード 256 B) に断片化**。
  クレジット制御 W=2 で in-flight を 536 B に抑える
- 追加 RAM は **MPI トランスポート 5.5 KB + SPP ログ 9.3 KB ≒ 15 KB** — RAM 残 330 KB に対して余裕

> Note: 「ヘッダを信じるな、リンクされるバイナリを信じろ」。ここは技術者ウケが良いはず。
> 300 / 672 / 512 の 3 つの数字を並べるのがこのスライドの山。

---

## 9. 実装の山場 ② — 実機は 1 発では動かない

**焼いて動かすまでに踏んだ地雷 (全部ログが残っている)**

> pptx には 5 個だけ載せてある (#3 走行中のリンク切断は口頭に回す)。

| # | 症状 | 原因 | 対処 |
|---|---|---|---|
| 1 | `finalize done` の 2 秒後に **MemFault で無限リブート** (17.5 秒周期) | RTX でスレッドが自分自身を終了させるとスケジューラが壊れる | スレッドを return させず **park** |
| 2 | ケースに入れて充電起動すると **BT スタックが上がらない** (`size=1` に縮退) | `CHARGER_PLUGINOUT_RESET=1` の副作用 | `target.mk` を **0** にパッチ |
| 3 | 走行中に **リンクが切れる** | 充電器の PLUGIN イベントが `CLOSE_BOX` を注入していた | 箱イベントを遮断 |
| 4 | 起動 15〜60 秒で **勝手に電源が落ちる** | `FULL_CHARGING` → `app_shutdown()` (満充電シャットダウン) | `app_shutdown()` を `if (0)` 化 |
| 5 | PC と **再ペアリングできない** | 通常ビルドは page scan だけで inquiry scan を出さない | 専用ビルドで discoverable |
| 6 | SPP ログの **先頭 512 B だけ二重に届く** | SDK が 1 回の RFCOMM open で `CONNECTED` を **2 回**上げる | 送信 FSM で重複を無視 |

**クラッシュログ (Run 2, 両バッズ・毎ブート再現)**

```
[mpi] finalize done rank=0
### EXCEPTION ###
CFSR =00000082   ; MMARVALID | DACCVIOL
MMFAR=00000034
FaultCause: (Data access violation)
Current Task    : 0     <- os_tsk.run == NULL = 走るタスクが居ない
New Running Task: 255   ; idle TCB
```

**`MMFAR = 0x34` が犯人を名指しした**: 0x34 は RTX の `OS_TCB` 構造体の `swap_out_time`
オフセットと**厳密に一致**する。`__RTX_CPU_STATISTICS__` が有効なため、スレッドが自己終了すると
`os_tsk.run` が NULL になった直後に `os_tsk.run->swap_out_time` を書きに行っていた。

**効いた運用**: 純ロジックは全部ホストのテストを先に書いた。実機で出た SPP のバグも
5 本のテスト (F9〜F13) に落としてから直した。
(#2〜#5 は SDK 側のパッチなのでホストテストの対象外)

> Note: 逆アセンブルではなく `nm` でシンボル解決して犯人を特定した、という小ネタも入る。
> 「組み込みは電源とペアリングで死ぬ」は共感ポイント。

---

## 10. 実装の山場 ③ — 観測をワイヤレスにする / 耳で操作する

**やりたかったこと**: USB ケーブルで繋がったイヤホンは「イヤホン」ではない。

**① Bluetooth SPP に自作ログチャネルを生やした**

- `COMPUTE_TRACE` のログ行をリングバッファに積み、**SPP (= Bluetooth 上のシリアルポート) で
  Windows に流す**
- PC 側は `spp_tail.py COM6` で受けるだけ。`#<seq>` 番号で**欠落・重複を機械検出**
- **UART とは独立の経路**なので、原理的に USB は要らない
  (※ 実測はすべて USB 接続下。「USB を抜いて動かした」ランはまだ取っていない)

**② 5 連タップで GEMM を再実行できるようにした**

- タッチパッドは BES2300YP の**電源キー入力**。ジェスチャは SDK がソフトで判定 (`hal_key.c`)
- 単タップ〜4 連・長押しは既存機能で埋まっていた → **未割当の `RAMPAGECLICK` (5 連)** を奪う
- 調停ロジックは**ホストで 8 テスト書いてから**実装した

```
 ① 5 連タップ          → ② 相手に START を送る    → ③ 相手も起きる      → ④ 両バッズで GEMM
   叩かれた側だけが         左右間の制御チャネルに       受信側は待機セマフォ     rank 0 が checksum 検証
   キーイベントを受け取る    5 バイトのフレームを 1 本    を解放して計算に入る     [mpi] run #1 done rank=0
```

実行中の連打は捨てる。相手の START が落ちたらタップした側だけが走り、
5 秒のストール検出を経て縮退で完走する。

> Note: デモ的に一番「おっ」となるところ。ケースの中でもタッチは反応する。

---

## 11. 結果 ① — 実機ログ (両バッズ / 全条件 PASS)

**起動時ラン (Run 4, 2026-09-01)**

```
右 (rank 0):
  CHARGING PWRON! → bt_stack_init_done:10
  [mpi] side=RIGHT rank=0 nv_role=[IBRT_MASTER] init_done=1
  [mpi] init rank=0 size=2 link_wait=200 ms besaud=1
  [mpi] peer ok rank=0 peer=1
  [mpi] barrier ok
  GEMM-MPI N=32 rank=0 size=2 checksum=32768.000000 expect=32768.000000 PASS
  GEMM-MPI elapsed=13 ms  frames tx=2 rx=4 err=0
  [mpi] finalize done rank=0

左 (rank 1):
  [mpi] init rank=1 size=2 link_wait=500 ms  → [mpi] send ok
  frames tx=2 rx=2 err=0 → [mpi] finalize done rank=1
```

**合否条件 — すべて充足**

- ✅ 両側 `size=2` — 縮退していない = 本当に 2 ノードで走った
- ✅ `checksum=32768.000000` **厳密一致** (float32 で丸め誤差ゼロの決定的ケース)
- ✅ rank 0 / rank 1 が左右で 1 つずつ (静的 rank が設計どおり)
- ✅ `### EXCEPTION ###` 0 回・MPI フレーム (cmdcode 0x8201) 送信失敗 0 回・リブート無し

> Note: 「32768 = 32³。全要素 1 の行列積は整数値しか通らないので float32 で厳密に一致する。
> 誤差 1 ulp も許さない判定にしてある」は必ず言う。なお機能検証そのものはホストの 1,788 checks
> とゴールデンリファレンス側が担っていて、実機はトランスポート込みの結合確認。

---

## 12. 結果 ② — ワイヤレス受信と 5 連タップ再実行

**Bluetooth SPP で PC が受けたログ (Run 12, 欠落・重複ゼロ)**

```
#0  [ctor] GlobalProbe constructed
#1  hello, C++ from PineBuds (core=0)
#2  GEMM float N=32  checksum=32768.000000  expect=32768.000000  PASS
#3  GEMM elapsed=9 ms
#7  [mpi] peer ok rank=1 peer=0
#16 [mpi-t1] probe len=512 ok rtt=124 ms
#18 [mpi-t1] rtt n=100 min=44 avg=169 max=407 ms
#19 [mpi-t1] max_payload=512
#20 [mpi] barrier ok
#23 [mpi] finalize done rank=1
```

**5 連タップ時の右バッズ (rank 0) UART (Run 13)**

```
app_key_compute_run event 12
[mpi] run #1 trigger=local peer_notified=1
GEMM-MPI N=32 rank=0 size=2 checksum=32768.000000 expect=32768.000000 PASS
GEMM-MPI elapsed=128 ms frames tx=1 rx=1 err=0
[mpi] run #1 done rank=0
[mpi] run #2 trigger=peer
GEMM-MPI elapsed=5 ms frames tx=1 rx=0 err=0
```

| run | トリガ | 結果 | elapsed |
|---|---|---|---|
| 起動時 | 自動 | PASS, size=2 | 12 ms |
| #1 | **右**を 5 連タップ | PASS, size=2 | 128 ms |
| #2 | **左**を 5 連タップ | PASS, size=2 | **5 ms** |
| #3 | **右**を 5 連タップ | PASS, size=2 | 115 ms |
| #4 | **左**を 5 連タップ | PASS, size=2 | 83 ms |

- タップした側が先に走り出し、相手は START の伝搬待ちで合流するので **タップした側 (rank 0) の
  elapsed が伸びる** (#1 / #3)
- **正直な注記**: このランで COM6 に出たのは**左 (rank 1) のログだけ**。IBRT の slave 側の SPP 送信は
  スタック上は成功扱いなのに電波に乗らないため、rank 0 の `GEMM-MPI` 行は UART 側にしか
  出ていない (**未解決**)

> Note: 「耳を 5 回叩くと両バッズで行列積が走り、実行ログが Bluetooth で PC に流れてくる」で締める。
> 「PASS 行そのものが飛んだ」とは言わない — 飛んだのは実行ログ (`run #n trigger/done`)。

---

## 13. 結果 ③ — 3 モード比較 (最新の Run 16, 2026-09-01 17:35〜17:45)

起動時ランと 5 連タップのランが、同じ N=32 の GEMM を **3 通りの構成で連続実行**して比較行を出す。

| mode | バッズ | コア | elapsed | vs single |
|---|---|---|---|---|
| `single` | 1 | 1 | 3,783 µs | 基準 |
| `mpi` | 2 | 各 1 | 3,683 µs | **x1.02** |
| `mpi+omp` | 2 | **各 2** | 313,258 µs | **x0.01** |

**並列領域そのもの: 3,783 µs → 1,226 µs (x3.1)** — 2 バッズ × 2 コア = 4 コアぶんの効果は出ている。
(1 コアで半分の行を回した場合の 3,783/2 ≒ 1,890 µs に対しても 1.54 倍)

**実機ログ (Bluetooth 経由で PC が受信、Run 16 タップ #2)**

```
#47 GEMM-MPI mode=mpi     elapsed=3683 us   frames tx=1 rx=0 err=0
#49 GEMM-MPI mode=mpi+omp elapsed=313258 us frames tx=1 rx=2 err=0
#50 [omp] last region: primary share=1226 us, extra wait for cp=0 us
#52 GEMM-MPI mode=single  elapsed=3783 us   frames tx=0 rx=0 err=0
#53 GEMM-CMP N=32 rank=0 size=2 threads=2 single=3783 us mpi=3683 us mpiomp=313258 us PASS
#54 GEMM-CMP rank=0 speedup vs single: mpi=x1.02 mpiomp=x0.01 worker_on_cp=3
```

**第 2 コアが動いた証拠**

- `worker_on_cp` がランごとに +1 — CP 上で `self_is_worker()==1` として並列領域が走った回数
- `extra wait for cp = 0 µs` — CP は MCU が自分の担当行を終える前に終わっている
- 3 モードとも `checksum=32768.000000` 厳密一致で PASS

**なぜ elapsed は 313 ms なのか**

- `mpi+omp` の elapsed が両側とも常に 313〜318 ms なのは、その **2 回目の Allreduce** が
  slave → master 方向のフレームを **省電力 (sniff) からの復帰まで約 300 ms 待つ**ため
- `mpi` はタップした側や直前のリンク状態で 3.7〜236 ms とばらつく。
  **x1.02 (タップ #2) がリンクが起きている時の MPI の実力** — 半分の行 + Allreduce 1 往復 ≒ single
- 比較ランの前に sniff を抜ける対策 (`app_tws_ibrt_exit_sniff_with_tws`) は実装済み。計測はこれから

**→ 計算は 4 コアぶん速くなった。が、その利得を Bluetooth の復帰待ちが丸ごと食った。**

> Note: ここが最大の山場。worker_on_cp が毎回 +1 しているのが第 2 コアが本当に走った証拠。
> 起動時ランは `app_sysfreq` を上げる前なので single が 15 ms (タップ時 3.8 ms の 4 倍)。
> 比較は同じラン内で閉じているので速度向上率には影響しない。

---

## 14. わかったこと / できていないこと

**わかったこと**

- **標準 API のまま実機に載る**: `bench/gemm_mpi_omp.cpp` は本物の OpenMPI でも自作アダプタでも
  無改変でビルドでき、3 経路すべて checksum 一致。**アダプタ境界の設計が正しかった**
- **ソースが無くても仕様は確定できる**: プリビルト `.a` の逆アセンブルで、ヘッダと食い違う
  実挙動 (上限 672、RX が BT スタックスレッド) を根拠付きで確定できた
- **ノード内 2 コア並列も動いた**: 第 2 コア (CP) で並列領域が走り、計算部分は 4 コアぶんの x3.1
- **組み込みの敵は計算ではない**: 6 個の地雷のうち計算に起因するものはゼロ。
  全部が電源・接続・RTOS・SDK の仕様
- **ホストで TDD できる形に切ると実機デバッグが激減する**: 実機で 1 回試す前にホストで
  1,788 checks。実機で出た SPP のバグもホストのテストに落としてから直した

**できていないこと (正直に)**

- **通信待ちを外す対策が未計測**: 比較ランの前に sniff を抜ける実装は入ったが、実機での効果はこれから
- **slave 側のログが電波に乗らない**: IBRT の snoop リンクでは slave の SPP 送信が
  スタック上は成功扱いなのに実際には飛ばない。rank 0 が master の時しか PC に届かない
- **音楽再生との同時実行は未検証**: 「イヤホンとして使いながら裏で計算」はまだ道半ば
- **N を大きくした時のスケーリングは未計測**: RAM 330 KB の上限は N≈128。
  そこまでで通信コストを償却できるかは未検証

---

## 15. まとめ

```
    ┌────────────────────────────────────────────────────────────┐
    │  1 万円のワイヤレスイヤホン 2 個 =  2 ノードの MPI クラスタ │
    └────────────────────────────────────────────────────────────┘

  ✅ 標準 MPI + OpenMP で書いたベンチが、無改変で耳の中で PASS した
  ✅ checksum=32768.000000 厳密一致 / 両バッズ size=2 / EXCEPTION 0 回
  ✅ 第 2 コアも動いた — 計算部分だけ見れば 4 コアぶんの x3.1
  ❌ 全体では速くならなかった — 利得を Bluetooth の復帰待ちが食った。理由は説明できる
```

**次にやること**

1. 通信待ちを外す — 比較ランの前に TWS リンクを省電力から起こす (実装済み・計測待ち)
2. 通信レイテンシに耐える問題を選ぶ: **Red-Black SOR** (ハロー交換は境界だけ = 通信量が小さい)
3. 音楽再生と計算の同時実行 — 本当に「使いながら計算」させる

**みなさんの耳にも、暇をしている Cortex-M4F が 2 個入っています**

**リポジトリ**: `github.com/TamichiRyuto/pine-buds-cluster`
設計・実機ログ・失敗の記録はすべて `docs/design-ibrt-transport.md` (2,643 行) にあります。

---

## 付録: 想定質問

1. **「N を大きくすれば通信を償却できるのでは？」**
   RAM 空き 330 KB で float32 の行列 3 枚を置くと N=128 が上限。そこまでで逆転するかは未計測。
   ただし Run 16 の 313 ms は計算量ではなく sniff 復帰なので、N を上げても定数として残る。
2. **「OpenMP は本当に 2 コアで走っているんですか？」**
   `worker_on_cp` が実行ごとに +1 する (CP 上で `self_is_worker()==1` として並列領域が走った回数)。
   `extra wait for cp = 0 µs` なので CP のほうが先に終わっている。並列領域は 3,783 → 1,226 µs。
3. **「RTT が sniff のせいなら sniff を切れば？」**
   sniff 制御はプリビルト `.a` の中で、公開ヘッダに叩ける API が無い。改善候補の筆頭。
4. **「checksum=32768 は全要素 1 でしょ。チートでも通るのでは？」**
   実機判定は精度非依存の決定的ケースとして意図的に選んだもの。機能検証はホストの 1,788 checks と
   本物の OpenMPI とのゴールデンリファレンス照合が担っている。
5. **「ESP32 を 2 個並べれば 10 分では？」**
   性能が目的なら PC 1 台でいい。狙いは「市販品として耳に入る完成品を、分解せず無改造のまま
   クラスタにできるか」。ペアリング済みの左右リンクにユーザーコードから相乗りしたのが技術的な点。
